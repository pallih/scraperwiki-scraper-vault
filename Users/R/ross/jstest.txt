var scraperwiki = require('scraperwiki');

scraperwiki.scrape( 'http://www.servercode.co.uk', function($, html){
    console.log( $('img').attr('src') );
});



var scraperwiki = require('scraperwiki');

scraperwiki.scrape( 'http://www.servercode.co.uk', function($, html){
    console.log( $('img').attr('src') );
});



var scraperwiki = require('scraperwiki');

scraperwiki.scrape( 'http://www.servercode.co.uk', function($, html){
    console.log( $('img').attr('src') );
});



var scraperwiki = require('scraperwiki');

scraperwiki.scrape( 'http://www.servercode.co.uk', function($, html){
    console.log( $('img').attr('src') );
});



